const writeCards= function(array){
  let names = ["Guadalupe", "Ollie", "Aki"]
  for (let item of array){
    return item
  }
  return `Thank you ${name} for the surprise gift`
}
// function writeCards(fun){
//   let names = ["Guadalupe", "Ollie", "Aki"];
//   fun (names);
  
// }